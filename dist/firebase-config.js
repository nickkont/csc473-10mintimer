"use strict";
/**
 * Eventra — Demo configuration (no real backend)
 *
 * Intentionally contains no real Firebase keys.
 * All account behaviour is front-end only — safe to share on GitHub.
 *
 * To wire up a real backend, replace this with environment-based config
 * that is NOT committed to a public repository.
 */
// Placeholder object to avoid reference errors in older code paths.
const firebaseConfig = {};
